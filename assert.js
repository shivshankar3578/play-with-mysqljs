const events = require('events');
const util = require('util');
const assert = require('assert');


console.log(assert.prototype);
// util.inherits(test.prototype, assert );

// test.ok(false, "error")
